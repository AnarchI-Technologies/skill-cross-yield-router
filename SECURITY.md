# Security

This skill is read-only and must not accept private keys or execute transactions.

Execution belongs in `cross-stake`, `cross-rewards`, `cross-dex-trade`, or `cross-treasury` after separate risk checks.
