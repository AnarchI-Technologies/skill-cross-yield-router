# Contributing

Keep the router deterministic and read-only.

- Separate observed yields from user-provided assumptions.
- Never imply an APR is guaranteed.
- Emit blockers when data is missing instead of inventing figures.
